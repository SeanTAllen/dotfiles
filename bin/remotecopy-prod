#!/bin/bash

source ~/bin/remotecopy

localfile=$1
user=$2
prefix=$3
hosts=$4
dest=$5
suffix=frontend.idc.theladders.com

for i in $hosts
do
  host=${prefix}-${i}.${suffix}

  putfile $host $user $localfile $dest
done


